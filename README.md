# 学生管理系统

一个基于 Spring Boot + Vue 3 的现代化学生管理系统，提供学生、教师、班级、课程和成绩的全方位管理功能。

## 📋 项目简介

本系统是一个功能完善的教育管理平台，支持多角色权限管理，提供直观的用户界面和完整的 RESTful API。系统采用前后端分离架构，具有良好的扩展性和维护性。

### 主要功能

- **用户管理**：支持管理员、教师、学生三种角色，基于 JWT 的身份认证
- **学生管理**：学生信息的增删改查、批量导入导出
- **教师管理**：教师信息维护、任课管理
- **班级管理**：班级信息管理、班主任分配
- **课程管理**：课程信息维护、学分管理
- **成绩管理**：成绩录入、查询、统计分析、Excel 导入导出
- **API 文档**：集成 Knife4j，提供在线 API 文档和测试功能

## 🏗️ 技术栈

### 后端技术

- **框架**：Spring Boot 3.1.5
- **安全**：Spring Security + JWT
- **数据库**：MySQL 8.0
- **ORM**：MyBatis Plus 3.5.4.1
- **API 文档**：Knife4j 4.5.0 (OpenAPI 3.0)
- **Excel 处理**：Apache POI 5.2.4
- **构建工具**：Maven

### 前端技术

- **框架**：Vue 3 (Composition API)
- **构建工具**：Vite
- **UI 组件**：Element Plus
- **状态管理**：Pinia
- **路由**：Vue Router 4
- **HTTP 客户端**：Axios

## 📁 项目结构

```
student-management-system/
├── Server/                          # 后端项目
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/student/
│   │   │   │   ├── config/         # 配置类
│   │   │   │   │   ├── SecurityConfig.java      # Spring Security 配置
│   │   │   │   │   ├── Knife4jConfig.java       # API 文档配置
│   │   │   │   │   └── WebMvcConfig.java        # Web MVC 配置
│   │   │   │   ├── controller/     # 控制器层
│   │   │   │   │   ├── AuthController.java      # 认证接口
│   │   │   │   │   ├── StudentController.java   # 学生管理
│   │   │   │   │   ├── TeacherController.java   # 教师管理
│   │   │   │   │   ├── ClassController.java     # 班级管理
│   │   │   │   │   ├── CourseController.java    # 课程管理
│   │   │   │   │   └── ScoreController.java     # 成绩管理
│   │   │   │   ├── service/        # 服务层
│   │   │   │   │   └── impl/       # 服务实现
│   │   │   │   ├── mapper/         # 数据访问层
│   │   │   │   ├── entity/         # 实体类
│   │   │   │   │   └── enums/      # 枚举类
│   │   │   │   ├── dto/            # 数据传输对象
│   │   │   │   ├── security/       # 安全相关
│   │   │   │   │   ├── JwtAuthenticationFilter.java
│   │   │   │   │   └── JwtAuthenticationEntryPoint.java
│   │   │   │   └── common/         # 公共类
│   │   │   │       └── utils/      # 工具类
│   │   │   └── resources/
│   │   │       ├── application.yml # 应用配置
│   │   │       └── mapper/         # MyBatis XML 映射文件
│   │   └── test/                   # 测试代码
│   └── pom.xml                     # Maven 配置
│
├── front/                          # 前端项目
│   ├── src/
│   │   ├── api/                    # API 接口
│   │   │   └── request.js          # Axios 配置
│   │   ├── assets/                 # 静态资源
│   │   ├── components/             # 公共组件
│   │   ├── layout/                 # 布局组件
│   │   │   └── MainLayout.vue      # 主布局
│   │   ├── router/                 # 路由配置
│   │   │   └── index.js
│   │   ├── stores/                 # Pinia 状态管理
│   │   │   └── user.js             # 用户状态
│   │   ├── views/                  # 页面组件
│   │   │   ├── login/              # 登录页
│   │   │   ├── dashboard/          # 仪表板
│   │   │   ├── student/            # 学生管理
│   │   │   ├── teacher/            # 教师管理
│   │   │   ├── class/              # 班级管理
│   │   │   ├── course/             # 课程管理
│   │   │   ├── score/              # 成绩管理
│   │   │   └── api-doc/            # API 文档
│   │   ├── App.vue                 # 根组件
│   │   └── main.js                 # 入口文件
│   ├── .env.development            # 开发环境配置
│   ├── .env.production             # 生产环境配置
│   ├── package.json                # 依赖配置
│   └── vite.config.js              # Vite 配置
│
└── database/                       # 数据库脚本
    └── student_management.sql      # 数据库初始化脚本
```

## 🚀 快速开始

### 环境要求

- **JDK**：17 或更高版本
- **Node.js**：16 或更高版本
- **MySQL**：8.0 或更高版本
- **Maven**：3.6 或更高版本

### 数据库配置

1. 创建数据库：
```sql
CREATE DATABASE student_management CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. 导入数据库脚本：
```bash
mysql -u root -p student_management < database/student_management.sql
```

3. 修改后端配置文件 `Server/src/main/resources/application.yml`：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_management?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: your_password  # 修改为你的数据库密码
```

### 后端启动

1. 进入后端目录：
```bash
cd Server
```

2. 安装依赖并启动：
```bash
mvn clean install
mvn spring-boot:run
```

或者在 IDE（如 IntelliJ IDEA）中直接运行主类。

后端服务将在 `http://localhost:8080` 启动。

### 前端启动

1. 进入前端目录：
```bash
cd front
```

2. 安装依赖：
```bash
npm install
```

3. 启动开发服务器：
```bash
npm run dev
```

前端服务将在 `http://localhost:3000` 启动。

## 👤 默认账号

系统提供了三种角色的测试账号：

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | admin | admin123 | 拥有所有权限 |
| 教师 | teacher | teacher123 | 可管理学生、班级、课程、成绩 |
| 学生 | student | student123 | 只能查看自己的成绩 |

## 📚 API 文档

系统集成了 Knife4j API 文档，启动后端服务后可访问：

- **Knife4j 文档**：http://localhost:8080/doc.html
- **OpenAPI JSON**：http://localhost:8080/v3/api-docs

在前端系统中，管理员和教师角色可以通过"API文档"菜单访问嵌入式文档。

### API 认证

大部分 API 需要 JWT Token 认证：

1. 调用登录接口获取 Token
2. 在请求头中添加：`Authorization: Bearer {token}`

## 🔑 核心功能说明

### 1. 用户认证与授权

- 基于 JWT 的无状态认证
- 三级权限控制（管理员、教师、学生）
- 密码使用 BCrypt 加密存储
- Token 有效期 24 小时

### 2. 学生管理

- 学生信息的 CRUD 操作
- 支持按姓名、学号、班级搜索
- 批量导入学生信息（Excel）
- 导出学生列表（Excel）
- 分页查询

### 3. 教师管理

- 教师信息维护
- 职称管理
- 任课信息关联
- 分页查询

### 4. 班级管理

- 班级信息管理
- 班主任分配
- 专业和年级管理
- 学生统计

### 5. 课程管理

- 课程信息维护
- 学分管理
- 课程类型分类
- 任课教师关联

### 6. 成绩管理

- 成绩录入和修改
- 多维度查询（学生、课程、班级）
- 成绩统计分析
- Excel 批量导入导出
- 学生端成绩查询

## 🛠️ 开发指南

### 后端开发

#### 添加新的 API 接口

1. 在 `entity` 包中创建实体类
2. 在 `mapper` 包中创建 Mapper 接口
3. 在 `service` 包中创建 Service 接口和实现
4. 在 `controller` 包中创建 Controller
5. 使用 Swagger 注解添加 API 文档

示例：
```java
@RestController
@RequestMapping("/api/example")
@Tag(name = "示例管理", description = "示例管理相关接口")
public class ExampleController {
    
    @GetMapping
    @Operation(summary = "获取列表", description = "获取示例列表")
    public Result<List<Example>> list() {
        // 实现逻辑
    }
}
```

#### 数据库迁移

修改 `resources/mapper/*.xml` 文件或使用 MyBatis Plus 的注解方式。

### 前端开发

#### 添加新页面

1. 在 `views` 目录下创建页面组件
2. 在 `router/index.js` 中添加路由
3. 在 `api` 目录中添加 API 调用方法
4. 更新侧边栏菜单（如需要）

示例：
```javascript
// router/index.js
{
  path: 'example',
  name: 'Example',
  component: () => import('@/views/example/ExampleList.vue'),
  meta: { title: '示例管理', icon: 'Document', roles: ['ADMIN'] }
}
```

#### API 调用

```javascript
import request from '@/api/request'

export const getExampleList = (params) => {
  return request.get('/api/example', { params })
}
```

## 📦 构建部署

### 后端打包

```bash
cd Server
mvn clean package -DskipTests
```

生成的 JAR 文件位于 `target/student-management-system-1.0.0.jar`

运行：
```bash
java -jar target/student-management-system-1.0.0.jar
```

### 前端打包

```bash
cd front
npm run build
```

生成的静态文件位于 `dist` 目录，可部署到 Nginx 或其他 Web 服务器。

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 前端静态文件
    location / {
        root /path/to/dist;
        try_files $uri $uri/ /index.html;
    }
    
    # 后端 API 代理
    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # API 文档
    location /doc.html {
        proxy_pass http://localhost:8080;
    }
    
    location /v3/api-docs {
        proxy_pass http://localhost:8080;
    }
}
```

## 🔧 配置说明

### 后端配置 (application.yml)

```yaml
server:
  port: 8080                        # 服务端口

spring:
  profiles:
    active: dev                     # 环境配置：dev-开发, prod-生产
  datasource:
    url: jdbc:mysql://localhost:3306/student_management
    username: root
    password: your_password
  
jwt:
  secret: your-secret-key           # JWT 密钥（生产环境请修改）
  expiration: 86400000              # Token 有效期（毫秒）

knife4j:
  enable: true                      # 启用 API 文档
  setting:
    language: zh_CN                 # 文档语言

# 日志配置
logging:
  level:
    root: INFO                      # 根日志级别
    com.student: INFO               # 应用日志级别
```

**环境切换**：
- 开发环境：`spring.profiles.active=dev` (显示详细日志和 SQL)
- 生产环境：`spring.profiles.active=prod` (只显示重要日志)

### 前端配置 (.env.development)

```env
VITE_API_BASE_URL=http://localhost:8080/api
```

## 🐛 常见问题

### 1. 后端启动失败

**问题**：数据库连接失败
**解决**：检查 MySQL 服务是否启动，配置文件中的数据库密码是否正确

**问题**：端口被占用
**解决**：修改 `application.yml` 中的端口号，或关闭占用 8080 端口的程序

### 2. 前端启动失败

**问题**：依赖安装失败
**解决**：删除 `node_modules` 和 `package-lock.json`，重新运行 `npm install`

**问题**：API 请求失败
**解决**：检查后端服务是否启动，检查 `.env.development` 中的 API 地址是否正确

### 3. 登录失败

**问题**：401 未授权
**解决**：检查用户名密码是否正确，检查数据库中用户数据是否存在

### 4. API 文档无法访问

**问题**：404 Not Found
**解决**：确认 Knife4j 依赖已正确添加，检查 Spring Security 配置是否放行了文档路径

### 5. Excel 导入导出失败

**问题**：文件格式错误
**解决**：确保使用 .xlsx 格式，检查模板格式是否正确

## 📝 更新日志

### v1.0.0 (2025-11-02)

- ✅ 完成基础功能开发
- ✅ 实现用户认证与授权
- ✅ 实现学生、教师、班级、课程、成绩管理
- ✅ 集成 Knife4j API 文档
- ✅ 实现 Excel 导入导出功能
- ✅ 优化前端界面和用户体验
- ✅ 修复枚举字段映射问题
- ✅ 优化表格布局和操作按钮
- ✅ 修复登录 401 错误
- ✅ 实现粘性表头功能

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

本项目采用 Apache 2.0 许可证。详见 LICENSE 文件。

## 👥 联系方式

- 项目维护者：开发团队
- Email：dev@example.com

## 🙏 致谢

感谢以下开源项目：

- Spring Boot
- Vue.js
- Element Plus
- MyBatis Plus
- Knife4j

---

**注意**：本项目仅供学习和研究使用，请勿用于商业用途。
